# Client / public Supabase configuration
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Server-only secret. NEVER prefix this with VITE_ and NEVER expose it to browser code.
# Only define it if a future server function needs Supabase service-role access.
SUPABASE_SERVICE_ROLE_KEY=
 
